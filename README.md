```
    naphy

    compiled with mingw
    all libraries for the native build are in lib/
    the dll's from lib/ still need to be copied to build/ before running
    no need to move res/ into build/ because CMakeLists is configured to do it automatically
```